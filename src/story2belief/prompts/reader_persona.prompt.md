<!--
prompt_id: reader_persona.v0
purpose: Render a ReaderProfile into a system prompt that conditions the
         agent's reading behavior.
inputs:
  - reader_profile (json) - serialized ReaderProfile
outputs:
  - text (str) - system-prompt body
written_for: gpt-4o-class models, used as system prompt
-->

# Reader-persona prompt (v0) -- placeholder

Replace this with the templating logic that turns a ReaderProfile into a
short, neutral persona description (no demographic stereotypes).
